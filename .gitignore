# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.venv/
venv/
.python-version

# Tooling
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# Runs and data
runs/
data/
# Fetched-data caches. `--cache DIR` stores gzipped API responses keyed by query;
# they are reproducible from the network, they carry their own age, and they are not
# ours to redistribute.
.cache*/
*.gpkg
*.tif
*.pbf
*.osm

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Source manuals. Licensed third-party documents (AASHTO HSM, iRAP Methodology
# Reference Guide). Read them, derive from them, cite them — never redistribute them,
# least of all from a public repository.
references/

# Design briefs — held back deliberately, not by accident.
# This repository is PUBLIC. Both documents contain pricing, commercial strategy,
# competitor positioning and a candid assessment of what is not yet defensible about
# the method. Remove these two lines to publish them.
PIPELINE_AND_DATA_BRIEF.md
ROAD_RISK_PANEL_BRIEF.pdf
